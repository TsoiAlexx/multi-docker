sudo: required
services:
 - docker

before_install:
 - docker build -t mark/react-test -f ./client/Dockerfile.dev ./client

script:
 - docker run -e CI=true mark/react-test npm test

after_success:
 - docker build -t multi-client ./client
 - docker build -t multi-nginx ./nginx
 - docker build -t multi-api ./api
 - docker build -t multi-worker ./worker

 #Log in to the docker CLI
 - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin

 # Take those images and push them to docker hub
 - docker tag multi-client mark/multi-client
 - docker tag multi-nginx mark/multi-nginx
 - docker tag multi-api mark/multi-api
 - docker tag multi-worker mark/multi-worker
 - docker push mark/multi-client
 - docker push mark/multi-nginx
 - docker push mark/multi-api
 - docker push mark/multi-worker